- key: f5
  command: workbench.action.reloadWindow

- key: tab
  command: editor.action.triggerSuggest
  when: >-
    editorLangId =~ /^jsonc?$/
    && textInputFocus
    && !editorReadonly
    && !suggestWidgetVisible
    && !editorHasSelection
