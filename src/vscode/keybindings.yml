- key: f1
  command: workbench.action.tasks.runTask
  args: Documentation

- key: tab
  command: editor.action.triggerSuggest
  when: >-
    editorLangId =~ /^jsonc?$/
    && textInputFocus
    && !editorReadonly
    && !suggestWidgetVisible
    && !editorHasSelection
